programa teste12
inicio
   
    inteiro: vetor[4];
    inteiro: k,x, i;
       
    k <- 4;
                          
    para i de 0 ate k passo 1 faca   /*Este laco faz a leitura de cada elemento do vetor*/  
       leia(vetor[i]);
    fim para;

    x <- 0;
    enquanto (x < 5) faca /*Este laco compara cada elemento do vetor*/
       leia(vetor[0]);
       x <- x + 1;
    fim enquanto;
    
   // escreva(' O maior numero que voce digitou foi ', x);

  //retorne 0;

fim.