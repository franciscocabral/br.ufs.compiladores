/* This file was generated by SableCC (http://www.sablecc.org/). */

package expression.node;

import java.util.*;
import expression.analysis.*;

@SuppressWarnings("nls")
public final class AAvalieEstruturas extends PAvalieEstruturas
{
    private PExpressao _expressao_;
    private final LinkedList<PCase> _blocoCaso_ = new LinkedList<PCase>();
    private final LinkedList<PComando> _blocoComando_ = new LinkedList<PComando>();

    public AAvalieEstruturas()
    {
        // Constructor
    }

    public AAvalieEstruturas(
        @SuppressWarnings("hiding") PExpressao _expressao_,
        @SuppressWarnings("hiding") List<?> _blocoCaso_,
        @SuppressWarnings("hiding") List<?> _blocoComando_)
    {
        // Constructor
        setExpressao(_expressao_);

        setBlocoCaso(_blocoCaso_);

        setBlocoComando(_blocoComando_);

    }

    @Override
    public Object clone()
    {
        return new AAvalieEstruturas(
            cloneNode(this._expressao_),
            cloneList(this._blocoCaso_),
            cloneList(this._blocoComando_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAAvalieEstruturas(this);
    }

    public PExpressao getExpressao()
    {
        return this._expressao_;
    }

    public void setExpressao(PExpressao node)
    {
        if(this._expressao_ != null)
        {
            this._expressao_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._expressao_ = node;
    }

    public LinkedList<PCase> getBlocoCaso()
    {
        return this._blocoCaso_;
    }

    public void setBlocoCaso(List<?> list)
    {
        for(PCase e : this._blocoCaso_)
        {
            e.parent(null);
        }
        this._blocoCaso_.clear();

        for(Object obj_e : list)
        {
            PCase e = (PCase) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._blocoCaso_.add(e);
        }
    }

    public LinkedList<PComando> getBlocoComando()
    {
        return this._blocoComando_;
    }

    public void setBlocoComando(List<?> list)
    {
        for(PComando e : this._blocoComando_)
        {
            e.parent(null);
        }
        this._blocoComando_.clear();

        for(Object obj_e : list)
        {
            PComando e = (PComando) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._blocoComando_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._expressao_)
            + toString(this._blocoCaso_)
            + toString(this._blocoComando_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._expressao_ == child)
        {
            this._expressao_ = null;
            return;
        }

        if(this._blocoCaso_.remove(child))
        {
            return;
        }

        if(this._blocoComando_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        if(this._expressao_ == oldChild)
        {
            setExpressao((PExpressao) newChild);
            return;
        }

        for(ListIterator<PCase> i = this._blocoCaso_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PCase) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        for(ListIterator<PComando> i = this._blocoComando_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PComando) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
