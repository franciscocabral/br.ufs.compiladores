programa teste15
inicio
   
    inteiro: a, b, c, d, e, f, z, y; 
    a,c,e <- 1;
    b,d,f,y <- 2;
    z <- 8;

    a += b;  // Resultado de a = 3
    c -= d;  // Resultado de c = -1
    e *= f;  // Resultado de e = 2
    z /= y;  // Resultado de z = 3

fim.