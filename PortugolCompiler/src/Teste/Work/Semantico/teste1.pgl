programa teste1
inicio
    
    const H 1;
    inteiro: x, y;
    real: x,z; // x duplicado
    caractere: z; //z duplicado
    x <- 1;
    y <- 2;
    y <- y + 3,14; //y é inteiro e não pode receber real
    
    p <- (3+ (2 * 2)); // variavel p não declarada
    
    escreva('teste');
	
	se (x < y) entao
	    escreva('Ok');
	fim se;	
        
fim.
