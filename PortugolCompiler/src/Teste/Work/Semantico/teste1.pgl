programa teste1
inicio
    
    inteiro: x, y;
    real: x,z; // x duplicado
    caractere: z; //z duplicado
    x <- 1;
    
    y <- y + 3,14; //y é inteiro e não pode receber real
    
    const e 1;  //e = e logico
    
    leia(e);  //Constante não pode ser lida
    
fim.
